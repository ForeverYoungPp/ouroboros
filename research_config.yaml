task: "Compare ARCHITECTURE.md loading strategies (current full-text / nav-map conversion / doc partitioning / hot-sections) in the ouroboros review-assembly lanes and implement the best one. Hard constraints: no content deletion (P1: indexing is relocation, not omission; acceptance: nav-map line ranges as ground truth, agent hits target section within <=K read_file calls, tokens/turns counted), P3 required fail-closed semantics must not be relaxed. INIT: extract campaign 4518 touched-path set from ~/Ouroboros/data/memory/consciousness_observations.json (~18-26 contracts files + ci.yml) to build the assembly fixture and compare token accounting (170,660 rendered row_cost -> ~766 nav map, ~211k freed). Prior ordering: #1 full nav-map coverage unlocks first -> #5 hybrid end-state; #4 partitioning is orthogonal to #1. Details: repo-root ARBOR_CONTRACT.md"
coordinator:
  max_cycles: 6
  max_depth: 2
  merge_threshold: 5.0
ui:
  interaction_mode: review
search:
  backends: []
executor_max_turns: 50
